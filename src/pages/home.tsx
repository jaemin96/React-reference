import { ReactElement } from 'react';
import { Timer } from '../components';

const Home = (): ReactElement => {
  return (
    <>
      <div>Home page</div>
      <Timer />
    </>
  );
};

export default Home;
